# BLING Blue Team — Pinned dependencies
# Run: pip install -r requirements.txt

# API
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
python-multipart==0.0.9

# Database
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9
asyncpg==0.29.0

# Neo4j
neo4j==5.20.0

# Redis / Celery
redis==5.0.4
celery==5.4.0
kombu==5.3.4
# APScheduler removed — replaced by Celery Beat (P0-2)

# ML
xgboost==2.0.3
shap==0.44.1
river==0.21.0
scikit-learn==1.4.2
faiss-cpu==1.8.0
pandas==2.2.2
numpy==1.26.4

# Retry / circuit breaker
tenacity==8.3.0

# Graph algorithms
networkx==3.3
joblib==1.4.2

# Phase 2 graph layer (P2-1 Leiden, P2-6 Node2Vec)
leidenalg==0.10.2
igraph==0.11.6
node2vec==0.4.6

# Phase 4 (P4-4 calibration, P4-6 XGBOD novelty)
pyod==2.0.2

# Tier 3 committee upgrade (Phase 0+)
mapie>=0.8.0                        # conformal calibration (Phase 2)
sentence-transformers>=2.7.0        # Scorer F multilingual remark screener
torch>=2.1.0                        # DeepSVDD anomaly discovery; future Mamba
tabpfn>=2.0                         # Scorer C few-shot prototype classification

# Observability
structlog==24.1.0
prometheus-client==0.20.0
prometheus-fastapi-instrumentator==7.0.0

# Rate limiting
slowapi==0.1.9

# JWT RS256 auth (P1-7)
PyJWT[crypto]==2.8.0

# HTTP client (for integration clients)
httpx==0.27.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
hypothesis==6.100.1
httpx==0.27.0
factory-boy==3.3.0

# Dev tools
ruff==0.4.4
