# Dev and test dependencies — NOT installed in production Docker image
# Run: pip install -r requirements-dev.txt

-r requirements.txt

pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
hypothesis==6.100.1
ruff==0.4.4
